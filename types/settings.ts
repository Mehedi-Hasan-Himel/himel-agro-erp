export interface FarmSettings {
  farmName: string;
  currency: string;
  currencySymbol: string;
  contactNumber: string;
  ownerName: string;
  establishedYear: number;
  location: string;
  notes?: string;
}
